# Multiplication Table Project

### What I've Learned

1. What HTML is
2. What CSS is, and how to use and nest it
3. Basic DOM element targeting (via CSS selectors)
4. Wrote some basic javascript containing variables, methods & loops

Additional notes: Learned how to use flexbox to help create spacing and alignment in this project vs. using HTML, learning that flex can be used as a shortcut. I had difficulty spotting errors in my code, especially forgetting to match the function title to my file name, which led my code to not render correctly (the grid would not generate). This was seen in my HTML and JS files. I used ChatGPT to help me spot those errors and was able to correct them. Also, I know the number of files I have is a bit much on my repo, but just to make sure you're looking at the right ones (to avoid confusion), it should be the flexbox-table.html and flexbox-table.js for the assignment. The CSS file should be normal besides those two. 
